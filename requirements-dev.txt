# it is okay to have requirements-dev.txt
# but not okay to have requirements.txt
# but please favor changing pyproject.toml instead of requirements-dev
# if the file gets too big

black==23.7.0
httpx==0.24.1
isort==5.12.0
mypy==1.4.1
pytest==7.4.0
pytest-asyncio==0.21.1
